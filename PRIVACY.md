# データの取扱い

## 検索

検索語は検索処理にだけ使用します。POST本文で受け取り、URL、D1、利用計測へ保存しません。Cloudflareの運用ログへ検索本文を明示的に出力する処理もありません。

## 宛先控え

利用者が保存した郵便番号・住所・カナは、そのブラウザの `localStorage` だけに最大60件保存します。アカウント、Cookie、外部同期はありません。ブラウザから削除できます。

## 最小限の計測

D1には、ブラウザで生成したランダムIDのSHA-256、許可済みの操作名、時刻、QA判定だけを35日間保存します。郵便番号、住所、検索語、IPアドレス、広告識別子は保存列にありません。第三者解析サービスは使いません。
