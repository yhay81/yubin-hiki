# Decisions

## 一般郵便番号から始める

一般住所の全国データだけで、番号と住所の双方向検索を完成させます。事業所個別番号とデジタルアドレスはデータ体系と利用場面が異なるため、実利用が確認できるまで混ぜません。

## 検索内容を保存しない

住所は生活圏や関係先を推測できる情報です。改善指標には検索実行、該当なし、コピー、保存、再訪の件数だけを用い、入力値や郵便番号を残しません。

## URLは郵便番号詳細だけ

検索はPOSTで行い、検索語をURLやアクセスログへ出しません。公開可能な7桁郵便番号だけを `/zip/:postal` の安定URLにします。

## アカウントを持たない

宛先控えは端末内だけで十分です。認証、同期、復旧を追加して住所データを集めるより、即時利用とデータ最小化を優先します。
