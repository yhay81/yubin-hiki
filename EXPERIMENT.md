# 35日評価

公開日から35日後に、実利用だけを対象として継続判断します。自動QAは `is_qa = 1` で分離します。

## 見る順序

1. 実利用者が検索まで進んだか
2. 該当なし検索が多すぎないか
3. 郵便番号または住所のコピーが使われたか
4. 宛先控えと再訪が起きたか

少数でもコピーや再訪がある場合は、データ更新の自動化と検索精度を改善します。訪問だけで検索がない場合は入口と検索例、検索はあるが該当なしが多い場合は正規化と候補提示、利用がない場合は露出経路を見直します。検索語そのものは収集しません。
